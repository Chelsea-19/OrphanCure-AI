# Data Sample Notes

This folder contains small public-safe benchmark samples and derived feature
tables for the demo. It does not include raw repoDB downloads, Open Targets API
caches, or the full PrimeKG graph.

To reproduce the full local benchmark, download repoDB and PrimeKG from their
original sources and run the scripts under `scripts/`.
